# Runtime dependencies for cg_api_analysis.
#
# Install with:
#   python -m venv venv
#   source venv/bin/activate          # Windows: venv\Scripts\activate
#   pip install -r requirements.txt

# --- Data retrieval (extract_data) ---
pycoingecko>=3.2.0      # CoinGecko API client
requests>=2.31.0        # HTTP transport used by pycoingecko

# --- Data handling (extract_data, chart_analysis) ---
pandas>=2.2.0           # DataFrames, CSV cache, correlation matrices
numpy>=1.26.0           # Finite/NaN checks in chart_analysis.correlation

# --- Plotting (chart_analysis) ---
matplotlib>=3.8.0       # Price charts

# --- Notebooks (optional: only needed for the .ipynb files) ---
ipykernel>=6.29.0       # Jupyter kernel
ipython>=8.18.0         # Interactive shell

# The test suite uses only the standard library (unittest), so it needs
# nothing beyond the packages above.
